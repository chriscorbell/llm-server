---
description: Rewrite the STATUS.md lines that a change made stale
argument-hint: "<what changed>"
---
Reality changed: $@

Update `STATUS.md` so it describes the current state, not the past one. Rewrite only the affected lines; do not restructure the file. If the change came from an experiment in `docs/log/`, link the entry from the line you change. If the change is a durable claim about this hardware or model, add it as a Finding with the measured numbers and the link. Write down the number, not the adjective. Show me the diff when done.
