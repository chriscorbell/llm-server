# 2026-09-13 Turbo Q6_K MTP2 on SYCL

Status: concluded
Profile: turbo-gguf

## Hypothesis

Two-token MTP will improve Turbo Q6_K's steady decode on SYCL without breaking text, tool calls or vision.

## Configuration

Change only `TURBO_SPEC_TYPE` from `none` to `draft-mtp`. The draft limit is already fixed at two, and the pinned GGUF contains its head. Keep the SYCL image, 65,536-token context, F16 KV, flash attention and all batch/sampling settings fixed.

```bash
ssh vllm 'cd ~/Code/llm-server && bash scripts/gpu-health.sh > scratch/turbo/before-mtp-health.log 2>&1'
ssh vllm 'cd ~/Code/llm-server && TURBO_SPEC_TYPE=draft-mtp bash scripts/compose.sh --profile turbo-gguf up -d --no-deps llama-turbo'
python3 eval/mtp_check.py --model qwen38-turbo --out scratch/turbo/sycl-mtp-short-checks.json
python3 scripts/bench.py --base-url http://vllm:8000 --model qwen38-turbo \
  --prompt-tokens 4096 --gen 256 --workload code --corpus-file scratch/turbo/bench-corpus.py \
  --warm --prompt-id turbo-profile-comparison -n 2 --json scratch/turbo/sycl-mtp-warm.json
```

Baseline: [SYCL without speculative decoding](2026-09-13-turbo-sycl.md), 19.7 tok/s and 0.214 s TTFT on 4,199-token warm code prompts, 256 generated tokens, concurrency 1 and thinking off. The benchmark now reads llama.cpp's unlabelled `llamacpp:spec_decode_*` counters using the same acceptance calculation as vLLM. The request corpus was frozen before that code edit.

## Measurements

The two warm repetitions use identical request hashes to the no-speculation baseline, with 4,199 actual input tokens, 4,195 cached tokens, 256 generated tokens, thinking off and concurrency 1:

| Metric | No speculation | MTP2 |
|---|---:|---:|
| Median decode | 19.7 tok/s | 34.8 tok/s |
| Median TTFT | 0.214 s | 0.224 s |
| Short checks | 18/18 | 18/18 |
| Draft acceptance | Not applicable | 85.6% |

MTP repetitions measure 35.0 and 34.7 tok/s. Position acceptance is 92.02% for the first draft token and 78.72% for the second. Near-limit context and coding/image checks are in progress. Raw benchmark data: `scratch/turbo/sycl-mtp-warm.json` on mbp.

The near-limit retrieval and continuation check passes 3/3, concurrency 1 and thinking off:

| Request | Actual input | Cached input | TTFT |
|---|---:|---:|---:|
| Cold retrieval | 63,997 tokens | 0 tokens | 94.030 s |
| Repeated retrieval | 63,997 tokens | 63,993 tokens | 0.628 s |
| Continuation | 64,143 tokens | 64,109 tokens | 1.433 s |

```bash
python3 eval/context_check.py --model qwen38-turbo --engine llama.cpp \
  --prompt-tokens 64000 --out scratch/turbo/context-64k.json
```

These checks verify exact retrieval across the window and cached continuation, not general coding quality at 64K.

Cold effective prefill is 680.6 tok/s at 63,997 input tokens, measured as input divided by TTFT. The cold and cached retrieval responses each generate 113 tokens at 29.4 tok/s, concurrency 1 and thinking off. The one-second VRAM sample reaches 28.080 GiB during this context check.

Pi 0.85.1, an isolated provider configuration, medium effort and concurrency 1:

| Task | Result | Time | Maximum actual prompt | Tool calls |
|---|---|---:|---:|---:|
| TypeScript retry bug | Pass | 27.9 s | 6,508 tokens | 5 |
| Screenshot layout correction | Pass | 28.1 s | 6,268 tokens | 2 |

Both tasks contain returned reasoning, 548 and 867 characters respectively, and pass the existing independent verifiers. The screenshot task sends the real image through Pi. The selected tasks pass 2/2; the remaining six task-suite cases were not run. The temporary Pi configuration uses alias `qwen38-turbo`, a 65,536-token window, 16,384-token output limit and the existing API key, without changing the installed client defaults. Results: `scratch/turbo/pi-tasks/summary.json` on mbp.

The sampled global peak over the measured MTP benchmark, long-context check and Pi tasks is 30,235,656,192 bytes, 28.159 GiB, at one sample per second. This is a sampled peak, not an instantaneous hardware maximum.

## What happened

The backend comparison passed 18/18 short checks on SYCL. The next startup enables the existing draft head, with no separate draft-model file.

MTP startup reaches the listening socket in 71.946 s. The log confirms `creating MTP draft context against the target model`, with no separate weight copy. All 18 short checks pass again, including three successive tool-call requests. The rendered-template token count is also checked against a completed request: 27 tokens for the addition case, matching `usage.prompt_tokens`. This validates the llama.cpp adapter in `eval/context_check.py` before the long run.

During the image task, llama.cpp emits warnings such as `find_slot: non-consecutive token position 4275 after 4275 for sequence 0 with 488 new tokens`. The image task still passes its layout verifier, the container remains healthy with zero restarts, and the GPU health capture shows no new kernel reset. The exact runtime log is preserved as `scratch/turbo/sycl-mtp.log` on the server; no engine restart was used to address these warnings.

## Outcome

Confirmed on the measured workload. MTP2 raises median code decode from 19.7 to 34.8 tok/s, about 77%, with 85.6% draft acceptance. Short checks pass 18/18, near-limit retrieval and continuation 3/3, and selected Pi coding/image tasks 2/2. Broader quality comparisons and context beyond 65,536 tokens remain unmeasured.

## Consequences

Selected `TURBO_SPEC_TYPE=draft-mtp` with `TURBO_DRAFT_TOKENS=2` in `compose/turbo.env`. The separate profile is ready to switch to using the root README. The original vLLM daily driver is restored after validation; no installed client default, kernel or host driver changes are required.

### Final deployment and restoration

The server pulled the committed SYCL/MTP2 defaults. Running `bash scripts/compose.sh --profile turbo-gguf up -d --no-deps llama-turbo` without shell overrides reported the existing container as `Running`, proving the checked defaults match the tested deployment. Turbo was healthy with zero restarts before stopping. Final diagnostics are in `scratch/turbo/final-turbo-health.log` on both machines and the full MTP runtime log is retained on both machines.

The original vLLM container `ff4da06641021d44bc7b468d8ee8fbb17bec4944914f240fe9fc6e8ae765939c` was renamed back and started at 04:43:48 UTC on September 13. It reused its compilation artifacts, 1.56 s of compilation reported, and began serving at 04:45:32 UTC. It is healthy with zero Docker restarts and the original 205,391-token KV capacity. Model discovery returns `qwen38` with `max_model_len: 131072`; a 19-token prompt returns exactly `pong`, two generated tokens. The watchdog remains active and SearXNG stayed up. No weights were removed.

The one-second VRAM sampler was stopped after validation. Temporary Pi credentials were removed; its nonsecret model/settings files and result transcripts remain under `scratch/turbo/` on mbp. `STATUS.md` now includes the durable Finding that this Q6_K profile works, with the measured speed, memory and capability limits.
