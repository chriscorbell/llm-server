# 2026-09-11 vLLM 0.29.0 serving comparison

Status: prepared, runtime not started
Profile: a-int4draft, MTP4, no CPU pinning

## Hypothesis

The released XPU engine and newer XPU kernels reduce serving overhead while preserving the current model, draft overlay, tool calling and vision behavior.

## Configuration

The image is the only variable relative to the MTP4 control in the [depth experiment](2026-09-11-int4-mtp-depth.md). Both arms use model revision `9d189a60`, FP16 KV, maximum context 98,304, utilization 0.95, batched tokens 8,192, one sequence, XPU graphs, prefix caching and the same four startup patches. Neither arm pins CPUs. Although the preceding drift recheck uses MTP3, the engine comparison is against MTP4 measurements on the old image.

Old image: `vllm/vllm-openai-xpu@sha256:f01e24f6c7ff01f1e0662234255a1372297d1dbd89d003cf13c8fad3eab1ba4f`, vLLM `0.27.2rc1.dev77+gac7509e2b.xpu`, XPU kernels `0.1.12.3`.

Candidate: `vllm/vllm-openai-xpu@sha256:1db27a8b75ae1d6b3cbf16ebbb88310c2b5548221c8df1335082b2a54dba209a`, vLLM `0.29.0+xpu`, XPU kernels `0.1.14.1`. Both use PyTorch `2.13.0+xpu`. The [isolated preflight](2026-09-11-vllm029-preflight.md) records package inspection and patch application before GPU use.

```bash
ssh vllm 'cd /home/chris/Code/llm-server && bash scripts/gpu-health.sh' > eval/results/2026-09-11-tuning/pre-v029-health.log 2>&1
ssh vllm 'cd /home/chris/Code/llm-server && env MTP_TOKENS=4 VLLM_IMAGE=vllm/vllm-openai-xpu@sha256:1db27a8b75ae1d6b3cbf16ebbb88310c2b5548221c8df1335082b2a54dba209a bash scripts/compose.sh --profile a-int4draft up -d --no-deps vllm-a-int4draft'
python3 eval/mtp_check.py --out eval/results/2026-09-11-tuning/v029-short.json
bash scripts/bench-arm.sh v029
```

## Measurements

Pending. Six measured repetitions per throughput workload, concurrency 1. Separate warm code, reasoning and tool output at about 8K input, plus warm code at 32K and 57K. Record actual token counts, sampled global peak VRAM, acceptance and TTFT. Correctness uses the repeated short-output gate followed by the corrected eight-task Pi suite at xhigh.

## Outcome

No runtime result yet.

## Consequences

The serving image pin remains unchanged until measurements support promotion.
