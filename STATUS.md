# Status

Last updated: 2026-09-23. Rewrite the affected lines whenever reality changes. This file describes the present, never the past.

## Current state

The complete MacBook Pi configuration now lives in [pi-config](https://github.com/chriscorbell/pi-config), checked out at `~/Code/pi-config`. This includes model definitions, the custom llm-server extension, global browser instructions, package pins, advisor/web/goal/todo preferences, Orca extension snapshots, GUI preferences, and the server-project prompt sources. Installed links point there. Offline migration checks preserve 11 extensions, 17 tools, 37 skills, 3 project prompts and the global prompt addition, with zero extension-loading errors. Restart existing Pi sessions to load the moved extension. [Migration](docs/log/2026-09-22-pi-config-migration.md)

Claude Code 2.1.280 on mbp runs against the server through `claude-qwen`, which links to `clients/claude-code/claude-qwen` and reads the key from `~/.config/llm-server/api-key`. Plain `claude` still uses the Anthropic login. The launcher points every model slot at `qwen38`, sets the window to 131,072 tokens and effort to xhigh. Headless and interactive runs pass a read, edit and test task at concurrency 1. Compaction, subagents and image input are untested. [Validation](docs/log/2026-09-23-claude-code-client.md), [client guide](clients/claude-code/README.md)

Codex on mbp now selects `qwen38` through the `vllm` Responses provider for new tasks. The desktop-bundled runtime passes file reading, code editing and 2/2 Node assertions with both an isolated configuration and the normal user configuration. Its app-server model list exposes `Qwen3.8-27B (vllm)` with Low, Medium and Xhigh. The 131,072-token window and 90,000-token compaction threshold are configured; compaction and vision have not been tested in Codex. Restart the desktop app and start a fresh local task to verify the visible picker. Restore prior OpenAI settings with `python3 clients/codex/select-provider.py openai`, then restart. [Setup and validation](docs/log/2026-09-20-codex-desktop-setup.md), [client guide](clients/codex/README.md)

Kernelogic's abliterated GPTQ checkpoint is the active healthy default under `a-int4draft`, with zero restarts. It passes 18/18 short checks, 8/8 Pi xhigh coding/image tasks and 3/3 xhigh retrieval/continuation checks at 123,975 to 124,510 input tokens. Warm decode is 87.1 tok/s with thinking off at 4,201 input tokens, and 67.9 tok/s at xhigh with 4,241 input tokens, both 384 generated tokens and concurrency 1. Cold near-limit TTFT is 142.581 s; cached repeat and continuation take 4.986 and 5.754 s. Sampled peak VRAM is 30.263 GiB. Select the existing `llm-server/qwen38` entry in Pi or OpenCode. Original model weights remain installed for rollback. [Deployment](docs/log/2026-09-13-uncensored-gptq.md)

The deployed Compose configuration contains only `a-int4draft` and `original-int4draft`. Both use the same vLLM image, FP8 KV, runtime INT4 draft, MTP4, vision, tools and 131,072-token window; their fixed `/model` mounts select the kernelogic and SergiioB checkpoints respectively. Both start healthy with zero restarts, expose 205,391 KV tokens and complete an authenticated request. The former `a-bf16kv`, `a-fp8kv`, `a-nospec` and `turbo-gguf` profiles are removed. Pi and OpenCode expose only the shared `llm-server/qwen38` API model. The original profile was validated, then the abliterated profile was restored. Model weights were not deleted. [Profile change](docs/log/2026-09-13-two-checkpoint-profiles.md)

The original SergiioB checkpoint validated the 131,072-token window with FP8 KV; these are its baseline results, not measurements of the new abliterated checkpoint. Retrieval and continuation pass 3/3 at 125,995 to 126,157 actual input tokens; the 125,995-token cold request starts in 145.924 s, and cached requests in 5.706 to 5.836 s, concurrency 1 and thinking off. The FP8 comparison at 96K passed 18/18 short checks and 8/8 Pi xhigh tasks, and the 128K configuration passes another 18/18 short checks. Pi, OpenCode and Hermes on mbp now declare 131,072 tokens. Final Pi code and image checks with the updated client pass 2/2. [Cache comparison](docs/log/2026-09-12-fp8-int4draft.md), [128K validation](docs/log/2026-09-12-context-128k.md)

The server retains the September 11 campaign's MTP4 default, original pinned engine and unrestricted CPUs. On the original checkpoint, identical requests confirmed a 6.6% warm code-decode gain at 8,240 input tokens, 768 generated tokens, concurrency 1 and thinking off; reasoning throughput is unchanged within the observed spread. [Exact-request confirmation](docs/log/2026-09-11-mtp-fixed-prompt-confirmation.md). The 0.29.0 image and one-CCX CPU pin did not provide material speed gains and were not retained. Pi remains at xhigh by default; `pi --thinking medium` is documented for small edits after two 8/8 task runs at each effort. [Effort comparison](docs/log/2026-09-11-pi-reasoning-effort.md).

Profile `a-int4draft` is configured for FP8 KV, INT4 draft, MTP4, vision, tools and xhigh thinking at 131,072 context. The replacement kernelogic container started September 13 at 22:45:36 EDT and is healthy with zero restarts, 205,391 KV tokens and both INT4 draft conversions. Authenticated model discovery, 18/18 short checks, 8/8 Pi xhigh coding/image tasks and 3/3 near-limit xhigh checks pass. A post-profile-change completion also returned the expected sentinel. The API remains `http://100.103.136.98:8000/v1`, with its existing key in private `compose/.env`. The original weights are assigned to the separate `original-int4draft` profile instead of a shell path override. [Deployment](docs/log/2026-09-13-uncensored-gptq.md), [profile change](docs/log/2026-09-13-two-checkpoint-profiles.md)

The engine remains pinned at `sha256:f01e24f6c7ff01f1e0662234255a1372297d1dbd89d003cf13c8fad3eab1ba4f`. The server uses utilization 0.95, 8,192 batched tokens, one sequence and XPU graphs. `compose/model.env` records both fixed checkpoint directories; `compose/tuning.env` retains FP8 KV and 131,072-token defaults. Shell variables override saved settings only for that invocation.

With the original checkpoint, Pi 0.85.1 on mbp passes all eight tasks, including image input. The previous OpenCode/API baseline also passed eight of eight capabilities. [Pi validation](docs/log/2026-09-07-pi-client-validation.md), [earlier baseline](docs/log/2026-09-07-task-suite-baseline.md)

OpenCode 1.18.30 on mbp exposes `llm-server/qwen38` as `Qwen3.8-27B GPTQ-INT4` at `http://vllm:8000/v1`, with a 131,072-token context window, 32,768-token output limit, image input and tool calling. Its explicit 98,304-token input budget activates a 20,000-token compaction buffer, giving a computed 78,304-token trigger. The installed CLI passes a reproduction that previously sent a request without compaction at 98,861 previous-response tokens. A real presentation task had accumulated 214,412 characters of tool output before overflowing, then recovered through automatic compaction; T3 continued to display Failed during that recovery. Readback confirms the busy T3-managed ai-builders runtime still holds the old limits; restart T3 after its presentation task finishes to load the installed input budget. [Context diagnosis](docs/log/2026-09-12-opencode-context-overflow.md), [input-budget fix](docs/log/2026-09-12-opencode-compaction-headroom.md) Explicit low, medium and xhigh variants are installed, with camel-case `options.reasoningEffort: "xhigh"` as the default. All three selected levels reach the outgoing API request, and two default/override checks also pass. T3 Code 0.0.40 was refreshed and its actual picker shows only Low, Medium and Xhigh, with Xhigh selected. [Picker fix](docs/log/2026-09-12-t3-opencode-reasoning-fix.md), [default-effort correction](docs/log/2026-09-12-opencode-default-effort.md). The credential is in `~/.local/share/opencode/auth.json` with mode 0600. On OpenCode 1.18.29, a streamed file-read tool round trip passed 1/1 in 16.26 s at concurrency 1, with 18,704 input tokens on the first step. Select it with `/models` or `opencode -m llm-server/qwen38`. [Authentication and verification](docs/log/2026-09-11-opencode-provider-auth.md), [setup guide](clients/opencode/README.md)

Hermes Desktop 0.21.1 on mbp uses the `vllm-server` custom provider at `http://100.103.136.98:8000/v1`, model `qwen38`, with the validated 131,072-token context window and model discovery enabled. It is the default for new chats. `model.streaming` is false because this server combines the `qwen3_xml` tool parser with the `qwen3` reasoning parser, a combination whose streaming path can return tool markup as text. A live Hermes one-shot at concurrency 1 and reasoning off completed a terminal tool call and returned `HERMES_TOOL_OK` in 11.6 seconds. [Hermes client validation](docs/log/2026-09-11-hermes-desktop-client.md), [setup guide](clients/hermes/README.md)

The repaired recovery watchdog is enabled and active, rechecked September 13 after the checkpoint switch. It requires three failed health checks and a GPU fault, saves diagnostics, allows ten minutes for initialization and stops after two recovery attempts per incident. Five offline recovery checks passed. Recovery from a new physical GPU failure has not yet been observed. [repair and deployment](docs/log/2026-09-07-watchdog-repair.md)

Pi 0.86.1 on mbp defaults to `llm-server/qwen38` with medium thinking, a 131,072-token context window, a 16,384-token response limit, and compaction above 106,496 estimated tokens. Requests to the local server carry `preserve_thinking: false`. The custom extension, model definition, and all current client settings are maintained in [pi-config](https://github.com/chriscorbell/pi-config). The September 20 package reduction measured a 13,254-token prefix; that is a historical baseline, not a measurement of the current setup. Full-session compaction improvements remain unmeasured. [Context pressure](docs/log/2026-09-20-pi-context-pressure.md), [Pi setup migration](docs/log/2026-09-22-pi-config-migration.md)

Pi retains seven pinned packages and four local extensions. The installed advisor config now selects `openai-codex/gpt-5.6-sol` as executor at medium effort and `anthropic/claude-fable-5-1` as advisor at high effort; activation remains opt-in. The advisor transport patch is maintained in `pi-config/scripts/patch-advisor.mjs`. Browser and web configuration, plus todo and goal preferences, are recorded in pi-config. [Client setup](https://github.com/chriscorbell/pi-config), [native browser validation](docs/log/2026-09-20-pi-native-browser.md), [web configuration](docs/log/2026-09-20-pi-account-free-web.md)

| | |
|---|---|
| Server | `vllm`, reachable as `ssh vllm` |
| GPU | Intel Arc Pro B70, Battlemage G31, PCI `4c:00.0`, device id `0xe223` |
| Usable VRAM | 31.9 GiB (`0x7f9000000` after stolen memory) |
| Driver | in-tree `xe`, kernel 7.0.0-31-generic, Ubuntu 26.04.1 |
| Userspace | Level Zero and OpenCL runtime 26.05.37020.3 from the Ubuntu archive |
| Host | Threadripper 3970X, 32 cores, 60 GiB usable RAM, 1.7 TB free on NVMe |
| Motherboard and RAM expansion | MSI TRX40 PRO WIFI (MS-7C60), four 16 GB DDR4-3200 UDIMMs and four empty slots; manufacturer maximum 256 GB, unbuffered DIMMs only. SMBIOS incorrectly reports 512 GB. [Hardware assessment](docs/research/2026-09-11-deepseek-budget-hardware.md) |
| Model storage | WD_BLACK SN7100 2 TB NVMe, approximately 1.7 TB free; enough capacity for one approximately 510 GB V4.1 checkpoint. Conversion scratch requirements depend on the selected format. |
| Network | two Intel I211 gigabit ports, Tailscale at `100.103.136.98`, LAN `10.0.0.10` |
| Engine | vLLM XPU, image pinned by digest in `compose/.env.example` |
| Model | `kernelogic/Qwen3.8-27B-Uncensored-GPTQ-Int4-sym-G128-MTP-BF16` at revision `bd3f8d56b9dc617c995ec5a3ececa486d1d064be` |

## Findings

Each Finding cites the Experiment that produced it. Do not add a Finding without one.

- **Claude Code needs no proxy: vLLM's `/v1/messages` serves it directly.** The running build returns thinking and tool-use blocks, streamed and not. Three client settings are required: a Bearer token via `ANTHROPIC_AUTH_TOKEN` (`x-api-key` gets 401), an effort of low, medium or xhigh (the user setting `high` gets a retried 500), and `CLAUDE_CODE_MAX_CONTEXT_TOKENS=131072` (otherwise 200,000 is assumed). A child launched from the Claude desktop app must also drop the inherited session variables or it gets 401. With these, a read, edit and test task passes in 26.5 to 30.7 s headless and 1 min 41 s interactive at xhigh, concurrency 1. [Validation](docs/log/2026-09-23-claude-code-client.md)
- **Splash on the M5 Pro MacBook is slower than the B70 server except for cached time to first token.** With identical requests at concurrency 1, warm thinking-off code decodes at 62.6 versus 91.3 tok/s at 4,174 input tokens and 58.8 versus 81.6 at 32,567; xhigh reasoning at 4,214 input is 51.9 versus 68.9. Cold TTFT is 101.7 versus 21.2 s at 32,431 input. Cached TTFT is 0.27 versus 2.39 s at 32,567 input. Sustained Mac load reaches heavy thermal pressure; 30 repetitions of the 4K code request gave a 58.3 tok/s median. Quality was not compared. [Comparison](docs/log/2026-09-22-splash-mbp-comparison.md)
- **Advisor reaches Fable 5.1 through Anthropic OAuth when it uses Pi's configured provider.** With Pi 0.86.1, pi-advisor-flow 0.7.0 and pi-anthropic-auth 3.0.1, changing advisor's transport to `modelRegistry.streamSimple` passes 2/2 live consultation paths and forwards high reasoning effort. The unpatched compatibility stream bypasses OAuth shaping. [Migration and measurements](docs/log/2026-09-21-pi-anthropic-auth.md)
- **The existing Responses endpoint supports Codex's basic coding-tool loop.** The desktop-bundled Codex 0.155.0-alpha.9.2 passes file reading, code editing and 2/2 Node assertions in each of two checks, at concurrency 1 and a configured 131,072-token window. One check includes the normal user configuration and the custom catalog. A catalog is required for Qwen to appear in the tested app-server model list. No translation proxy or server change was needed. Desktop UI interaction, vision and compaction remain untested. [Setup and validation](docs/log/2026-09-20-codex-desktop-setup.md), [initial API check](docs/log/2026-09-20-codex-provider-compatibility.md)

- **The abliterated kernelogic checkpoint runs with the existing INT4 draft profile.** Pinned GPTQ INT4 revision `bd3f8d56` passes 18/18 short checks, 8/8 Pi xhigh tasks including vision, and 3/3 xhigh retrieval/continuation checks at 123,975 to 124,510 input tokens. Both INT4 draft conversions work; MTP4, FP8 KV, prefix caching and 131,072 context are retained. At concurrency 1 and 384 generated tokens, identical requests give warm code medians of 87.1 versus 87.0 tok/s at 4,201 input tokens, thinking off, and xhigh medians of 67.9 versus 73.6 tok/s at 4,241 input tokens. Each arm has three repetitions; this does not establish a stable speed difference or broad coding-quality equivalence. The abliterated checkpoint is selected, with original weights retained for rollback. [Deployment](docs/log/2026-09-13-uncensored-gptq.md)

- **The retired Turbo Q6_K configuration ran with xhigh thinking, vision, tools and 131,072 context.** Q8_0 KV on pinned SYCL b10920 passed 18/18 short checks and 2/2 Pi xhigh tasks at 64K before the separate context increase. At 128K it passed 3/3 xhigh retrieval/continuation checks at 123,975 to 124,943 input tokens and another 2/2 Pi xhigh coding/image tasks. Warm xhigh decode was 34.1 tok/s at 4,239 input tokens, 384 generated and concurrency 1; sampled peak was 29.26 GiB. The Compose profile was removed on September 13. [Cache comparison](docs/log/2026-09-13-turbo-thinking-q8kv.md), [128K validation](docs/log/2026-09-13-turbo-thinking-128k.md), [profile change](docs/log/2026-09-13-two-checkpoint-profiles.md)

- **Set an explicit OpenCode input budget to leave room for tool results.** With Qwen context 131,072 and declared output 32,768, `limit.input: 98304` activates the client's 20,000-token compaction buffer. The installed OpenCode 1.18.30 reproduction changes from sending the next build request directly to summarizing first, then continuing. This protects against the measured late-compaction case; arbitrarily large tool batches can still exceed the reserve. [Diagnosis](docs/log/2026-09-12-opencode-context-overflow.md), [installed fix](docs/log/2026-09-12-opencode-compaction-headroom.md)

- **Qwen needs explicit OpenCode reasoning variants.** T3 Code 0.0.40 invents four fallback choices when a model has no variants, but those choices do not set effort. The installed low, medium and xhigh map uses camel-case `reasoningEffort` and passes 3/3 transport checks. The explicit xhigh default and low override pass 2/2 further checks. T3's refreshed picker shows exactly the three supported levels. The running server defaults to xhigh if effort is absent and rejects high. [Original diagnosis](docs/log/2026-09-12-t3-opencode-reasoning-picker.md), [installed fix](docs/log/2026-09-12-t3-opencode-reasoning-fix.md), [default-effort correction](docs/log/2026-09-12-opencode-default-effort.md)

- **Use `gputop`, not `intel_gpu_top`, to monitor this `xe` device.** `intel_gpu_top` 2.3-1 defaults to an i915-only device search. Explicitly selecting `card0` finds the B70 but still fails because it expects i915-style per-engine `*-busy` PMU events. `sudo gputop` successfully reports per-process engine activity and GPU memory from DRM fdinfo. [Xe GPU monitoring](docs/log/2026-09-11-xe-gpu-monitoring.md)

- **`btop` 1.4.6-2 does not detect the B70 under `xe`.** The installed build has GPU support enabled, but its Intel collector is hard-coded to the `i915` PMU path and the live UI has no GPU panel. Ubuntu offers `nvtop` 3.2.0-2, whose upstream release added explicit `xe` support, but it has not been installed or checked locally. [btop check](docs/log/2026-09-11-btop-xe-monitoring.md)

- **Medium effort cuts completed-task time on the small fixture suite.** At concurrency 1, with per-task maximum prompts from 7,138 to 12,689 actual tokens, two runs per effort averaged 139.4 s for medium versus 211.15 s for xhigh. Both passed 16/16 task instances. This does not establish parity on difficult repository changes or compaction; xhigh remains the default, with `pi --thinking medium` documented for routine edits. [Effort comparison](docs/log/2026-09-11-pi-reasoning-effort.md)

- **Pinning to `0-3,32-35` does not improve this workload.** Three alternating pairs with six measured warm code repetitions per arm, about 8.3K input, 768 generated tokens, thinking off and concurrency 1 gave 90.3 tok/s unrestricted versus 89.6 pinned. Pair changes were +0.3%, -1.0% and -4.2%. Unrestricted CPUs remain selected. Docker update ignores an empty cpuset, so restore all online CPUs explicitly during live experiments and recreate to restore empty metadata. [CPU affinity](docs/log/2026-09-11-cpu-affinity.md)

- **vLLM 0.29.0 is compatible but provides no material measured speed gain here.** Its default XPU V2 runner executes both INT4 draft conversions and passes 18/18 short checks and 8/8 Pi tasks. At concurrency 1, warm code/tool/reasoning decode overlaps the old image's spread, cached TTFT falls about 30 ms, and cold 32.6K TTFT is 18.550 versus 18.606 s. It still needs four patches and holds 1,435 fewer KV tokens. The original digest remains selected. [Serving comparison](docs/log/2026-09-11-vllm029-serving.md)

- **MTP4 improves code throughput with the INT4 draft.** With all six complete request hashes matching across depths, warm code at exactly 8,240 input tokens and 768 generated tokens measures 90.1 tok/s versus MTP3's 84.5, a 6.6% gain, thinking off and concurrency 1. Xhigh reasoning at 8,282 input tokens is 65.0 versus 65.7 tok/s, inside the observed spread. Earlier 32K/57K code and 8K tool screening also favored MTP4 but used differing session nonces. Cold and cached TTFT are unchanged. Short checks pass 18/18 and corrected Pi tasks 8/8; no broader quality claim is established. [Exact-request confirmation](docs/log/2026-09-11-mtp-fixed-prompt-confirmation.md), [initial depth sweep](docs/log/2026-09-11-int4-mtp-depth.md)

Historical `scripts/bench.py` context labels used a character-based estimate. Their timings are measured, but those input lengths and derived prefill rates are approximate. New benchmark output records `usage.prompt_tokens`. [measurement correction](docs/log/2026-09-07-ttft-actual-token-counts.md)

- **The 16-bit KV profile cannot reach 128K on this card.** vLLM's own estimate is 109,824 tokens at 0.96 utilization, and the FP16 rollback configuration uses 98,304. The real cost is 76 KiB per token rather than the 64 KiB the layer arithmetic predicts, because speculative decoding buffers and the Gated DeltaNet recurrent state also come out of that budget. [first boot](docs/log/2026-09-07-profile-a-first-boot.md)
- **FP8 KV with the INT4 draft buys context at a measured latency cost.** At 96K maximum context it holds 191,566 KV tokens versus FP16's 109,067. Both pass 18/18 short checks, 3/3 checks at about 90K input and 8/8 Pi xhigh tasks. At concurrency 1 and thinking off, cold TTFT rises 14.6% at about 31K input and 19.6% at 89,987 input tokens. This small sample does not establish a code-decode speed change or quality parity on hard long-context coding. The selected 128K configuration allocates 205,391 KV tokens and passes retrieval/continuation at 125,995 to 126,157 input tokens. [Cache comparison](docs/log/2026-09-12-fp8-int4draft.md), [128K validation](docs/log/2026-09-12-context-128k.md)
- **The running server defaults to Qwen's thinking sampling preset.** Live generation config and startup logs confirm temperature 1.0, top_p 0.95, and top_k 20. An earlier 512-token, concurrency-one experiment associated explicit top_k 20 with higher acceptance and decode, but omission must not be assumed to disable top_k on this instance. Verify the effective request before attributing an effect. [client contract inspection](docs/log/2026-09-07-agent-client-contract.md), [earlier experiment](docs/log/2026-09-07-decode-speed-and-mtp-acceptance.md)
- **A larger prefill chunk buys nothing and costs context.** Doubling `MAX_BATCHED_TOKENS` to 16,384 left prefill identical at 1,402 tok/s at 64K and took 17,205 tokens of KV capacity. Prefill is limited by per-token work, not by how it is grouped. Do not retry this. [prefill chunk size](docs/log/2026-09-07-prefill-chunk-size.md)
- **The `performance` CPU governor changes nothing.** Boost already holds cores at 4.34 GHz under load. Left at schedutil. [governor and pinning](docs/log/2026-09-07-cpu-governor-and-pinning.md)
- **oneCCL needs `/dev/dri` bind mounted, not just device mapped.** Without it the engine dies at startup with `opendir failed: could not open device directory`, even on a single GPU. [first boot](docs/log/2026-09-07-profile-a-first-boot.md)
- **Decode speed is set by how predictable the output is, not the prompt.** Free prose gives 44.8 tok/s at 40% draft acceptance, writing new code gives 56.3 at 56%, and reproducing a file verbatim gives 85.2 at 100%. Swapping the prompt between code, prose and nonsense moves it by about 2 tok/s. [what drives decode speed](docs/log/2026-09-07-what-actually-drives-decode-speed.md)
- **Long context costs time to first token, not decode.** Decode falls gently with context: 42.8 tok/s at 512 prompt tokens, 37.2 at 32K, 28.4 at 90K, with acceptance flat near 40%. That tracks memory bandwidth. Time to first token is the real cost, reaching 82 seconds at 90K. The earlier claim of 18 tok/s at 28K was an artifact of an engine counter that includes prefill time. [decode versus context](docs/log/2026-09-07-decode-versus-context.md)
- **Prefix caching is the main defence against long context.** At 33.4K actual prompt tokens, thinking on, concurrency 1 and 128 generated tokens, cached TTFT is 0.811 seconds versus 19.211 cold (medians of three repetitions). Pi's cached tool follow-ups also started in 0.9 to 1.0 seconds at about 23K input tokens. Compaction and changed reasoning settings can require a fresh prefill. [current benchmark](docs/log/2026-09-07-ttft-actual-token-counts.md), [Pi check](docs/log/2026-09-07-pi-client-validation.md)
- **FP8 prefix caching uses 1,664-token blocks; the FP16 baseline used 832.** The larger blocks can leave more prompt text to prefill: an identical 31,015-token request reused 28,288 tokens with FP8 versus 29,952 with FP16, with cached TTFT 2.358 versus 0.893 s at concurrency 1 and thinking off. The cache share remains available in `usage.prompt_tokens_details.cached_tokens`. [Cache comparison](docs/log/2026-09-12-fp8-int4draft.md)
- **Warming the compacted context while idle removes the cold post-compaction request.** After a manual compaction at 21.8K to 31.3K tokens the next request started in 1.03 s with 29,952 tokens cached, against 11.71 s cold without the warm-up, one run per arm at concurrency 1 and xhigh. Threshold compaction that fires mid-run is unaffected, because the cold request follows before the client can act. [extension and warm-up](docs/log/2026-09-07-pi-quality-of-life.md)
- **The INT4 draft overlay is worth 26 to 31% decode at no measured quality cost.** Six repetitions per size, thinking on, identical prompts: 45.5 to 57.2 tok/s at 512 tokens, 45.6 to 58.5 at 8K, and 46.2 to 59.2 at 32K with a slightly different corpus. Per-position acceptance fell from 0.716, 0.494, 0.358 to 0.679, 0.472, 0.326. The eight-task Pi suite passed 8 of 8 in 202 seconds against 362 at the earlier baseline. Quality parity on hard repository work is unmeasured on both arms. [INT4 draft overlay](docs/log/2026-09-07-int4-draft-overlay.md)
- **MTP depth 3 beat depth 4 with the unquantized draft.** At six repetitions per size with thinking on and concurrency 1, decode medians were 48.5 against 45.3 tok/s at about 625 actual prompt tokens, and inside noise at about 7.6K and 33.4K. Per-position acceptance for the first three drafts barely moved (0.716, 0.494, 0.358), and removing the fourth draft returned KV capacity from 103,326 to 105,640 tokens. This comparison predates the INT4 draft overlay; it does not establish the best depth for the current profile. The later INT4 sweep separately measured depths 2, 3 and 4. [MTP depth 3](docs/log/2026-09-07-mtp-depth-3.md)
- **Decode runs about 30% under the bandwidth ceiling, and one CPU thread is pegged.** The vLLM engine core loop holds 99.5% of one core while the GPU stays at its 2800 MHz boost clock. That single-threaded loop is normal for vLLM, but on this XPU stack it may be the limiter. [decode versus context](docs/log/2026-09-07-decode-versus-context.md)
- **opencode 1.18.27 cannot send images to this server.** It attaches files with mime `text/plain`, so the model receives a text file and correctly says it cannot see an image. Vision itself works: score it with `eval/vision_check.py`, which sends a proper image part. Also note `--file` is a greedy option, so the message must come before `--file=<path>`. [task suite](docs/log/2026-09-07-task-suite-baseline.md)
- **This build returns thinking in `message.reasoning`.** Not `message.reasoning_content`. A client reading only the older field sees empty reasoning and a correct answer. [first boot](docs/log/2026-09-07-profile-a-first-boot.md)
- **Pi 0.85.1 works with the existing server.** All eight coding/image tasks passed. A separate ten-request check confirmed sampling, reasoning replay, medium/xhigh effort and requirement retention after automatic compaction. This validates the client contract and small-task behavior, not broad coding quality. [Pi validation](docs/log/2026-09-07-pi-client-validation.md)
- **The watchdog's false-trigger bug is repaired.** The old expression accepted arbitrary text. The installed version rejects unrelated network messages, remembers a single fault through the failure threshold and bounds recovery attempts. Five offline scenarios passed on Linux; the inference container was not restarted during deployment. [repair](docs/log/2026-09-07-watchdog-repair.md), [original evidence](docs/log/2026-09-07-watchdog-false-triggers.md)

Why this stack was chosen at all, including the engines rejected and the reasoning about Xe2 having no native FP8: [hardware survey and stack choice](docs/log/2026-09-07-hardware-survey-and-stack-choice.md).

## Configuration in force

Machine-specific values and credentials are in `compose/.env` on the server, template in `compose/.env.example`. `scripts/compose.sh` loads that file followed by the tracked measured defaults in `compose/tuning.env`. Shell environment overrides take precedence for experiments. The decisions behind these values:

The running service uses maximum context 131,072 tokens, FP8 KV, GPU utilization 0.95, batched tokens 8,192, one sequence, MTP4, INT4 draft overlay on and prefix caching on. KV capacity at 128K is 205,391 tokens. FP8 at 96K held 191,566; the pre-experiment FP16 baseline had 109,067. The retired `a-bf16kv` profile used `--dtype float16 --kv-cache-dtype auto`; its attention cache followed FP16. Old log labels retain that name for traceability. The example environment matches the working context and utilization values. [inspection](docs/log/2026-09-07-agent-client-contract.md)

- **GPTQ INT4, group size 128, symmetric, with selected Gated DeltaNet projections and the fifteen MTP tensors stored in BF16.** This is the working memory and speed choice. Published quality results for other 4-bit artifacts do not establish BF16 parity for this exact GPTQ checkpoint and XPU runtime. [upstream quality audit](docs/research/2026-09-07-agentic-coding-upstream.md)
- **Vision on.** Achieved by omitting `--language-model-only`. Costs about 0.92 GiB for the unquantized F16 vision tower.
- **`--max-num-seqs 1`.** Single-user server. This sidesteps the `gdn_attention` crash on mixed speculative and non-speculative batches, so `patch_gdn_mixed_split_v5.py` stays unapplied. Local coding-suite acceptance was 66 to 69%; output predictability matters, and a single sequence does not guarantee 95% acceptance.
- **INT4 draft overlay on, as profile `a-int4draft`.** The draft's LM head copy and its five MTP linears are requantized to INT4 g128 at container start by two vendored, env-gated patches. The target model and its verification head are untouched. Decode 57 to 59 tok/s against 45 to 46 for the BF16 draft on identical prompts at 512 to 32K context, task suite 8 of 8, and 5,869 more KV tokens because the freed BF16 draft weights go to the cache. Single card only; the patches refuse tensor parallelism. [INT4 draft overlay](docs/log/2026-09-07-int4-draft-overlay.md)
- **MTP with 4 speculative tokens for the INT4 draft.** The identical-request comparison gives 90.1 versus 84.5 tok/s for MTP3 at 8,240 input tokens, thinking off, 768 generated and concurrency 1. Reasoning is unchanged within spread. Longer-context screening measured 81.0 tok/s at 32,590 input tokens and 72.3 at 56,909. Cold 32K TTFT is unchanged at about 18.61 s. The retired unquantized-draft profile used MTP3; both retained runtime-INT4-draft profiles use MTP4. [Exact-request comparison](docs/log/2026-09-11-mtp-fixed-prompt-confirmation.md), [depth sweep](docs/log/2026-09-11-int4-mtp-depth.md)
- **Thinking on by default at `xhigh` reasoning effort.**
- **`UR_L0_ENABLE_RELAXED_ALLOCATION_LIMITS=1`.** Level Zero rejects single allocations above 4 GiB by default and the KV cache exceeds that.
- **Bound to the Tailscale address only**, with a static API key.

## Open questions

Ideas not yet tested. Move one into `docs/log/` the moment you test it.

- Chris's model-selection priority is agentic capability and intelligence. Which feasible deployment completes difficult repository tasks most reliably, including work across compaction? The existing eight small fixtures cannot rank the current GPTQ checkpoint against another model or higher precision. Establish that quality baseline before prioritizing further decode tuning. [Model comparison](docs/research/2026-09-11-local-model-alternatives.md)
- Can Qwen3.8-Flash-Next run correctly on the B70 with routed experts in host RAM and the 51B n-gram table on NVMe? It is the only model that fits today's memory with vendor scores above Qwen3.8-27B (DeepSWE 58.7 versus 42.2), but only llama.cpp offloads experts on Intel Arc, its `qwen4exp` support has no published SYCL or Vulkan run, and MTP is a draft PR. GLM-5.3-Flash (Terminal-Bench 2.1 84.3) needs the 192 GB upgrade. [MoE offload candidates](docs/research/2026-09-11-moe-offload-candidates.md)
- Can DeepSeek V4.1 Flash run correctly here with both Engram and expert weights offloaded to RAM/NVMe? A new CPU fork reports operation on a 512 GB Xeon, with reference correctness unvalidated. DwarfStar's author reports unreleased SSD streaming on a 128 GB Mac. Neither establishes a complete Intel deployment, but four 96 GB NVIDIA GPUs are not a minimum requirement. Validate runtime correctness and actual memory use before buying RAM or a replacement platform. [Offload assessment](docs/research/2026-09-11-deepseek-v41-offload.md), [hardware assessment](docs/research/2026-09-11-deepseek-budget-hardware.md)
- Does INT4 draft depth 5 improve on depth 4 while preserving reasoning throughput and the selected 131,072-token window? The current sweep stops at depth 4; use identical request hashes and check complete task quality before expanding the depth.
- Would persisting the engine compile cache across container recreation shorten configuration changes? Both image trials compile inside the container; no persistent-cache configuration has been tested. Ordinary container restarts retain their writable layer.

- How does this setup score on repeated, representative repository tasks beyond the seven small coding fixtures? The Pi compaction check preserves a simple requirement, but complex edits across compaction remain unmeasured. `scripts/bench.py` now records actual API prompt-token counts.
- Does the extension's 24 KB tool-output budget omit information needed for harder repository edits? The September 11 eight-task suite passes with the extension loaded, but broad quality under truncation remains unmeasured. [current task results](docs/log/2026-09-11-int4-mtp-depth.md), [extension and warm-up](docs/log/2026-09-07-pi-quality-of-life.md)
- Can the current FP8/INT4-draft configuration reliably serve 163,840 tokens? The validated 128K configuration reports 205,391 cache tokens, but 160K has not been tested. General coding quality near 128K also remains unmeasured; the long-prompt check tests exact retrieval and continuation. [128K validation](docs/log/2026-09-12-context-128k.md)
- Is the INT4 draft overlay still a win at 64K to 96K context? Measured only to 32K here. The upstream author saw the gain shrink to 18% at 96K and invert on a single cold 128K turn. [INT4 draft overlay](docs/log/2026-09-07-int4-draft-overlay.md)

## Setup gotchas

These break a fresh install and are handled by `scripts/setup-server.sh`. Recorded because each one cost time. [setup gaps](docs/log/2026-09-07-setup-script-gaps.md)

- Ubuntu 26.04 ships Docker without the Compose plugin. Install `docker-compose-v2` or every command fails with `docker: unknown command: docker compose`.
- The API is published on the Tailscale address only, so `http://127.0.0.1:8000` does not answer even on the server itself. Use the hostname.
- The `huggingface_hub` 1.x package dropped the `[cli]` extra and its entry point is not on `PATH` for a mapped container uid. Download through the Python API, which is what the setup script does.
- The checkpoint ships only `processor_config.json`, not the three image-processor files the upstream cookbook lists. Vision works with just that one.

## Known hazards on this hardware

- The `xe` driver can reset a compute engine under sustained load and wedge the context. Capture `scripts/gpu-health.sh` output before restarting anything.
- oneDNN's 2026 release notes carry a known issue that FP8 matmul may sporadically produce incorrect results on Arc B-series. This is one reason the plan avoids FP8 weights.
- Re-packed or locally converted copies of the model can drop the three image-processor JSON files, after which vLLM dies at startup with `Can't load image processor for '/model'`. `scripts/setup-server.sh` checks for them.
