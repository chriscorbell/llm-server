# Measured serving defaults. scripts/compose.sh loads this after the private .env.
# Shell environment overrides still take precedence for individual experiments.
# Keep credentials and machine-specific paths in .env.
MTP_TOKENS=3
CPUSET=
