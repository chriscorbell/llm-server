The attached screenshot shows this page rendered in a browser at 1280 by 800. Something
is visibly wrong with the layout.

Look at the screenshot, describe the defect in one sentence, then fix it in
`styles.css`. Do not change `index.html`, and do not change the sidebar's width or the
fact that it is fixed to the left edge.
