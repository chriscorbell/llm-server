The pagination helper in `paging.py` returns wrong results at the edges. Three tests
in `test_paging.py` fail.

Find and fix the bug. Run the suite with
`python3 -m unittest discover -s . -p 'test_*.py'`. Do not change the tests.
