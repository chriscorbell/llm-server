The test suite in this project fails. `src/collections.ts` is missing the `groupBy`
function that `test/collections.test.ts` expects.

Implement `groupBy` so the whole suite passes. Run the tests yourself to confirm.
Tests are run with `node --test --experimental-strip-types`.
