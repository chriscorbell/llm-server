`npm test` is not configured here; run the tests with
`node --test --experimental-strip-types`.

Two tests in `test/retry.test.ts` fail. Find the bug in `src/retry.ts` and fix it.
Do not change the tests.
